import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.psoftware.appmovil',
  appName: 'appmovil',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
